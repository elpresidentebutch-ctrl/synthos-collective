{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "dockerfilePath": "../../Dockerfile"
  },
  "deploy": {
    "startCommand": "/usr/local/bin/rpcnode",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
