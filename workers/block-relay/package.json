{
  "name": "synthos-block-relay",
  "version": "1.0.0",
  "description": "Block relay worker — receives finalized blocks from validators and serves a live feed to dashboards",
  "main": "src/index.js",
  "license": "Apache-2.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
